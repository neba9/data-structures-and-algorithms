def left_join(left_t,right_t):
  lists = []
  for i in left_t:
    index = left_1[i]
    if j in right_t:
      value = righ_t[index ]
    if i[index ] == j[index]
        lists= lists +  [[i, left_t[index ], value ]]
    else: 
            lists = lists +  [[j, none,right_t, value ]]
  return lists
  